{{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.description) }}
    {{ form_row(form.categorie) }}
    {{ form_row(form.imageFileThumbnail) }}
    {{ form_row(form.imageFilePicture) }}
    {{ form_row(form.isActive) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
